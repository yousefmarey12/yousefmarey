import { PhotoURLsDirective } from './photo-urls.directive';

describe('PhotoURLsDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoURLsDirective();
    expect(directive).toBeTruthy();
  });
});
